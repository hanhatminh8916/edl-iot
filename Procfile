web: curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared && chmod +x cloudflared && echo "Starting cloudflared tunnel..." && ./cloudflared access tcp --hostname mysql.hatrustsoft.me --url localhost:3306 & echo "Waiting 15s for tunnel to establish..." && sleep 15 && echo "Starting Spring Boot application..." && java -Dserver.port=$PORT -jar target/BFE_forAIOT-0.0.1-SNAPSHOT.jar
